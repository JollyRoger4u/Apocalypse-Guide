/*import React, { Component } from 'react';
import {Event} from "./event"
class EventHandler extends Component {

    fetchEvent = () =>{
        let currEvent = this.props.gameState.currentEvent;      
        let fetchedEvent = currEvent 
        return fetchedEvent;
    }
    
    render() { 
        let event = <React.Fragment>
            <Event id={this.fetchEvent()} />
        </React.Fragment>
        return ( event );
    }
}
 
export {EventHandler};*/